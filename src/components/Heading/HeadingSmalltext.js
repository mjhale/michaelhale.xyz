import styled from 'styled-components';

import theme from 'src/utils/theme';

const StyledHeadingSmalltext = styled.span`
  font-display: swap;
  font-family: ${theme.font.robotoFontFamily};
  font-size: 1.525rem;
  font-weight: normal;
`;

export default StyledHeadingSmalltext;
